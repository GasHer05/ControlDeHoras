rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Esta app usa autenticación propia (usuarios en Firestore),
    // NO Firebase Authentication. Por eso no se puede usar request.auth.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
